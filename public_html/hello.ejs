<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="./style.css" type="text/css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <h1 class="text-center text-6xl font-black text-green-900 bg-blue-200"><%= title %></h1>
    </header>

    <div role="main" class="text-center">
        <p class="my-10"><%= content %></p>
        <form action="/other" class="my-10" method="POST">
            <input type="text" name="msg" class="border-opacity-10 shadow py-2 bg-white-200">
            <input type="submit" value="送信" class="bg-indigo-700 font-semibold text-white py-2 px-4 rounded hover:bg-red-500 border-opacity-10 shadow">
        </form>
    </div>


    <div class="text-center">
        <table class="table">
            <% for (var key in data) { %>
            <tr>
                <th><%= key %></th>
                <td><%= data[key] %></td>
            </tr>
            <% } %>
        </table>
    </div>
</body>
</html>